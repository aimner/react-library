export { setBooking, changeBooking, cancelBooking } from './booking-api';

export { setComment, changeComment } from './comments-api';

export  { getBooks, getBook, getCategories } from './data-api';

export { registration, authorization, forgotPassword, recoveryPassword } from './auth-api';

export { getProfile, getAvatar, changeProfile } from './profile-api';

